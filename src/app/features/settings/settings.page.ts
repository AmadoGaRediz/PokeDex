import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { CollectionStore } from '../../core/services/collection.store';
@Component({selector:'app-settings-page',changeDetection:ChangeDetectionStrategy.OnPush,template:`
<header><p>RESPALDO Y DATOS</p><h1>Configuración</h1></header><section class="panel"><h2>Exportar colección</h2><p>Descarga un JSON con Pokémon obtenidos, favoritos y fechas.</p><button class="primary" (click)="store.exportJson()">Exportar progreso</button></section><section class="panel"><h2>Importar colección</h2><p>Restaura un respaldo compatible. Los registros importados reemplazarán coincidencias locales.</p><label class="file">Seleccionar archivo JSON<input type="file" accept="application/json,.json" (change)="importFile($event)"></label>@if(message){<p class="message">{{message}}</p>}</section><section class="panel"><h2>Actualizar Pokédex</h2><p>Consulta PokéAPI y refresca nombres, tipos, generaciones e imágenes sin borrar tu progreso.</p><button (click)="store.syncPokemon()" [disabled]="store.syncing()">{{store.syncing()?'Actualizando '+store.progress()+'%':'Actualizar ahora'}}</button></section>
`,styles:[`header p{color:var(--red);font-weight:900;letter-spacing:.2em}header h1{font-size:3.5rem;margin:0 0 25px}.panel{background:var(--surface);border:1px solid var(--line);border-radius:20px;padding:25px;margin-bottom:16px;box-shadow:var(--shadow)}.panel p{color:var(--muted)}button,.file{display:inline-block;border:1px solid var(--line);background:var(--surface-2);color:var(--text);padding:12px 18px;border-radius:12px;font-weight:800;cursor:pointer}.primary{background:var(--red);color:white;border:0}.file input{display:none}.message{font-weight:800}`]})
export class SettingsPage{readonly store=inject(CollectionStore);message='';async importFile(event:Event){const input=event.target as HTMLInputElement;const file=input.files?.[0];if(!file)return;try{await this.store.importJson(file);this.message='Respaldo importado correctamente.';}catch{this.message='El archivo no es un respaldo válido.';}finally{input.value='';}}}
