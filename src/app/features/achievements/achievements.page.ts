import { ChangeDetectionStrategy, Component, computed, inject } from '@angular/core';
import { CollectionStore } from '../../core/services/collection.store';
@Component({selector:'app-achievements-page',changeDetection:ChangeDetectionStrategy.OnPush,template:`
<header><p>MEDALLAS Y LOGROS</p><h1>Tu vitrina, {{store.trainerName()}}</h1><span>{{unlocked()}} de {{store.achievements().length}} desbloqueados</span></header>
<section class="grid">@for(item of store.achievements();track item.id){<article [class.unlocked]="item.unlocked" [attr.data-tier]="item.tier"><div class="medal"><span>{{item.icon}}</span></div><section><small>{{item.tier}}</small><h2>{{item.title}}</h2><p>{{item.description}}</p></section><b>{{item.unlocked?'DESBLOQUEADO':'BLOQUEADO'}}</b></article>}</section>
`,styles:[`header{margin-bottom:28px;padding:28px;border-radius:28px;background:linear-gradient(135deg,var(--surface),color-mix(in srgb,var(--red) 14%,var(--surface)));border:1px solid var(--line);box-shadow:var(--shadow)}header p{color:var(--red);font-weight:950;letter-spacing:.2em}header h1{font-size:clamp(2.4rem,8vw,4.6rem);margin:0}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(290px,1fr));gap:14px}article{position:relative;display:grid;grid-template-columns:72px 1fr;align-items:center;gap:16px;padding:18px;background:var(--surface);border:1px solid var(--line);border-radius:22px;opacity:.46;filter:grayscale(1);overflow:hidden}article:before{content:'';position:absolute;inset:auto -30px -70px auto;width:150px;height:150px;border-radius:50%;background:color-mix(in srgb,var(--line) 40%,transparent)}article.unlocked{opacity:1;filter:none;box-shadow:var(--shadow)}.medal{width:72px;height:72px;border-radius:50%;display:grid;place-items:center;background:linear-gradient(145deg,#c06b2d,#f6c588);box-shadow:inset 0 -6px 0 #0002,0 8px 18px #0002}.medal span{font-size:2.1rem}article[data-tier='silver'] .medal{background:linear-gradient(145deg,#aeb7c2,#f6f7f8)}article[data-tier='gold'] .medal{background:linear-gradient(145deg,#d49c16,#ffe38a)}article[data-tier='platinum'] .medal{background:linear-gradient(145deg,#6fc0d6,#f4fdff)}article[data-tier='master'] .medal{background:linear-gradient(145deg,#7d4de6,#ffd86b)}small{text-transform:uppercase;color:var(--red);font-weight:950;letter-spacing:.16em}h2,p{margin:2px 0}h2{font-size:1.05rem}p{color:var(--muted);font-size:.88rem}b{grid-column:2;color:var(--muted);font-size:.7rem}.unlocked b{color:var(--success)}@media(max-width:520px){.grid{grid-template-columns:1fr}article{grid-template-columns:62px 1fr}.medal{width:62px;height:62px}}`]})
export class AchievementsPage{readonly store=inject(CollectionStore);readonly unlocked=computed(()=>this.store.achievements().filter(x=>x.unlocked).length);}
